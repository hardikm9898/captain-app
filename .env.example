# Optional build-time fallback for the outlet's local server. At runtime the app
# first tries the address that worked last time, then the fixed mDNS name
# http://billerpe-local-server.local:4100, then this value, and finally lets the
# captain type the server PC's LAN address on the login screen.
VITE_EXE_BASE_URL=http://localhost:4100
